/*
module.exports = function() {
	local_store()
	for each uplink
		status = uplink_store()
		if (status == 201) {
			succeess.push(uplink)
		} else {
			bail()
		}

	bail:
		local_revert()
		uplink_revert()
};
*/
