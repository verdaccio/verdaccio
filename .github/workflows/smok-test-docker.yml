name: Docker Smoke Test
on:
  workflow_dispatch:
  push:
permissions:
  contents: read

concurrency:
  group: e2e-docker-pkgs-${{ github.ref }}-7.x
  cancel-in-progress: true  

jobs:
  smoke:  
    uses: verdaccio/verdaccio/.github/workflows/x-smok-test-docker.yml@ci-refactor
    with:
      docker_tag: '7.x-next'

