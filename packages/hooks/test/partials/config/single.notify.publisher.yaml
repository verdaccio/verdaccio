notify:
  method: POST
  headers: { 'Content-Type': 'application/json' }
  endpoint: http://slack-service/foo?auth_token=mySecretToken
  content: '{"publisher": "{{publisher.name}}", "publishedPackage": "{{publishedPackage}}", "publishType": "{{publishType}}"}'
