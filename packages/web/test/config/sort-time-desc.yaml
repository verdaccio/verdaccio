auth:
  auth-memory:
    users:
      test:
        name: test
        password: test

web:
  title: verdaccio web
  sort_field: time
  sort_packages: desc

uplinks:

log: { type: stdout, format: pretty, level: trace }

packages:
  '@*/*':
    access: $anonymous
    publish: $anonymous
  '**':
    access: $anonymous
    publish: $anonymous

_debug: true
