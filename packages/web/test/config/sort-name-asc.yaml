auth:
  auth-memory:
    users:
      test:
        name: test
        password: test

web:
  title: verdaccio web
  sort_field: name
  sort_packages: asc

uplinks:

log: { type: stdout, format: pretty, level: trace }

packages:
  '@*/*':
    access: $anonymous
    publish: $anonymous
  '**':
    access: $anonymous
    publish: $anonymous

_debug: true
