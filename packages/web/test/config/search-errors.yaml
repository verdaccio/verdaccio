auth:
  auth-memory:
    users:
      test:
        name: test
        password: test

web:
  title: verdaccio

uplinks:
  failing:
    url: https://failing.registry.test/
    retry: 0
  healthy:
    url: https://healthy.registry.test/
    retry: 0

log: { type: stdout, format: pretty, level: trace }

packages:
  'blocked-*':
    access: $authenticated
    publish: $anonymous
  '**':
    access: $anonymous
    publish: $anonymous

_debug: true
