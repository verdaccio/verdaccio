uplinks:
  timeout:
    url: https://registry.timeout.com/
    timeout: 2s
packages:
  'timeout':
    access: $all
    publish: $authenticated
    proxy: timeout
log: { type: stdout, format: pretty, level: info }
