uplinks:
  timeout:
    url: https://registry.domain1.com/
    timeout: 2s
packages:
  'timeout':
    access: $all
    publish: $authenticated
    proxy: timeout
log: { type: stdout, format: pretty, level: info }
